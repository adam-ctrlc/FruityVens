// Expo Go stub — onnxruntime-react-native requires a custom dev build with native code.
// Metro resolves this file instead of the real package so the JSI install() crash is avoided.
// When InferenceSession is null, FruitDetectionService throws a clean FruitDetectionError.
module.exports = {
  InferenceSession: null,
  Tensor: null,
};
